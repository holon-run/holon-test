schema = "holon.agent_template.v1"
id = "test-reviewer"
name = "Test Reviewer"
summary = "Minimal reviewer template fixture hosted in holon-test for sync/install smoke tests."

[compatibility]
holon = ">=0.1.0"
